import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.toto.infotainment',
  appName: 'Toto Infotainment',
  webDir: 'dist'
};

export default config;
